/**
 * Initialises a new set of permission classification or verifies an existing
 * set for completeness. The classifier uses a source org to export available
 * permissions (standard- and custom) to intiailise the classification files.
 */
export default class PermissionsClassifier {}
