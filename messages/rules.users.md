# violations.no-other-apex-api-logins

Has %s logins with "Other Apex API", which is a deprecated and insecure login type.

# violations.inactive-since-n-days

User is inactive for %s days (last login was %s).

# violations.has-never-logged-in

User was created %s (%s days ago), but never logged in.

# violations.entity-unknown-but-used

The %s is used, but classified as UNKNOWN. Cannot audit user role.

# violations.entity-not-classified-but-used

The %s is used, but not classified in %ss policy.

# violations.entity-not-allowed-for-user-role

User has the role "%s", but the assigned %s is classified as "%s". This is not allowed.
